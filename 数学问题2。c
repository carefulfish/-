/*有一个函数，定义如下：
           2x-1      (x<0)
y=f(x)=    2x      (10>x>=0)
           3x+1    (x>=10)
*/
#include<stdio.h>
int f(int x)
{int y;
 if(x<0) y=2*x-1;
 else if(x<10) y=2*x;
      else y=3*x+1;
 return y;
}
main()
{int x;
 scanf("%d",&x);
 printf("%d",f(x));
}
