//某书店卖书时有以下规定：购书在20000元以上者，大7.5折；购书在10000元以上者，大8折；购书在2000元以上者，大8.5折；购书在100元以上者，大9折。假设某一种书的定价为24元，编写一个程序，计算一位顾客只购买这种书的应付款数。
#include<stdio.h>
main()
{float x,s;int y;
 x=24;
 scanf("%d",&y);
 s=24*y;
 if(s<100) s=s;
 else if(s<2000) s=s*0.9;
      else if(s<10000) s=s*0.85;
	       else if(s<20000) s=s*0.8;
		        else s=s*0.75;
printf("%f",s);
}
